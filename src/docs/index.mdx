import {
  PlayGround,
  Knobs,
  PropsTable,
} from "react-display-window/lib/components";
import ThemeToggle from "react-color-scheme-switch";

# ThemeToggle

The central component of `react-color-scheme-switch`.

## Example

The most basic usage (without attached behavior) is:

<PlayGround>
  <ThemeToggle />
</PlayGround>

## Props

### PropsTable

This will document all the props the component accepts.

<PropsTable component={Button} />

### Knobs

This will render the component and some controllers to edit it's props in real time.

<Knobs component={Button} defaults={{ category: "primary" }}>
  <Button>My button</Button>
</Knobs>
